<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:tools="http://schemas.android.com/tools"
        xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable name="item" type="com.fazemeright.myinventorytracker.database.InventoryItem"/>
        <variable name="bag" type="com.fazemeright.myinventorytracker.database.BagItem"/>
        <variable name="clickListener" type="com.fazemeright.myinventorytracker.itemlist.ItemListAdapter.ItemListener"/>
    </data>
    <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_margin="@dimen/spacing_normal"
            app:cardBackgroundColor="@{bag.bagColor}"
            android:background="@drawable/curved_edges"
            android:onClick="@{() -> clickListener.onClick(item)}"
            android:layout_height="wrap_content">
        <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">
            <TextView
                    tools:text="Item Name"
                    android:layout_width="0dp"
                    android:text="@{item.itemName}"
                    android:textColor="@android:color/white"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_normal"
                    android:id="@+id/tvItemName"
                    app:layout_constraintEnd_toStartOf="@+id/tvBagName"
                    app:layout_constraintHorizontal_chainStyle="spread_inside"
                    app:layout_constraintTop_toTopOf="parent" app:layout_constraintStart_toStartOf="parent"
                    android:layout_marginStart="@dimen/spacing_small"/>
            <TextView
                    tools:text="Bag Name"
                    android:layout_width="0dp"
                    app:layout_constraintEnd_toEndOf="parent"
                    android:layout_height="wrap_content"
                    app:setBagName="@{bag.bagName}"
                    android:layout_marginTop="@dimen/spacing_normal"
                    android:textColor="@android:color/white"
                    android:id="@+id/tvBagName" app:layout_constraintStart_toEndOf="@+id/tvItemName"
                    android:layout_marginStart="@dimen/spacing_small" app:layout_constraintTop_toTopOf="parent"/>
            <TextView
                    tools:text="Item Quantity"
                    android:layout_width="0dp"
                    android:text="@{String.valueOf(item.itemQuantity)}"
                    android:layout_height="wrap_content"
                    android:id="@+id/tvItemQuantity"
                    android:textColor="@android:color/white"
                    android:layout_marginBottom="@dimen/spacing_normal"
                    app:layout_constraintStart_toStartOf="@+id/tvItemName"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintTop_toBottomOf="@+id/tvItemName"
                    android:layout_marginTop="@dimen/spacing_normal"/>

            <ImageView android:layout_width="wrap_content" android:layout_height="wrap_content"
                       android:src="@drawable/ic_delete"
                       android:layout_marginEnd="@dimen/spacing_normal"
                       android:onClick="@{() -> clickListener.onDeleteClick(item)}"
                       app:layout_constraintEnd_toEndOf="parent" android:layout_marginBottom="@dimen/spacing_normal"
                       app:layout_constraintBottom_toBottomOf="parent"
                       android:contentDescription="@string/delete_icon_content_desc"/>
        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>